include ../partials/variables.pug
doctype html
html
  head
    title Portfolio | ikgron
    include ../partials/head.pug
    include ../partials/styles.pug
    
    style. 
      .container {
        text-align: center;
      }

body
  include ../partials/navbar.pug
  div.container

    .portfolio-grid
      .portfolio-item
        a(href=`/list-sorter/`)
          img(src=`/res/images/sorter.png` alt=`Project 1` loading=`lazy`)
          .portfolio-info
            h1 List Sorter
            p A simple sorter that organizes items into a list by their first character or length. Recognizes characters from different languages.

      .portfolio-item
        a(href=`/`)
          img(src=`/res/images/homepage.png` alt=`Project 2` loading=`lazy`)
          .portfolio-info
            h1 www.ikgron.com
            p This website 👍             

  hr

include ../partials/scripts.pug
