include /partials/variables.pug
doctype html
html(lang=`en`)
  head
    title Tools | ikgron
    include /partials/head.pug
    include /partials/styles.pug

body
  include /partials/navbar.pug

  h1(style=`text-align: center;`) Tools

  div(style=`text-align: center; margin-top: 2rem`)
    button#toolsButton.btn(type=`button` class=`bttn` onclick=`window.location.href="/tools/list-sorter"`) List Sorter

  hr

include /partials/scripts.pug
