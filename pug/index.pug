include /partials/variables.pug
doctype html
html(lang="en")
head
  title Home | ikgron
  include /partials/head.pug
  include /partials/styles.pug

.bg
.vignette
.content
  include /partials/navbar.pug
  - const currentPage = 'home'
  
            
  h1.text-light(style=`margin-top: 15vh; font-size: ${lfont}; margin-left: 2vh`) Home

  h1.text-light(style=`margin-top: 1vh; font-size: ${sfont}; margin-left: 2vh`) A cool website by ikgron

  div(style=`border-bottom: 2px solid ${color}; margin-top: 15vh;`)




include /partials/scripts.pug
