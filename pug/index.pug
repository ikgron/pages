include /partials/variables.pug
doctype html
html(lang="en")
head
  title Home | ikgron
  include /partials/head.pug

.bg
.vignette
.content
  include /partials/navbar.pug
  
            
  h1.text-light(style="margin-top: 11vh; font-size: 3rem; margin-left: 1vh") Home

  h1.text-light(style="margin-top: 1vh; font-size: 2rem; margin-left: 1vh") A personal website by ikgron

  div(style=`border-bottom: 2px solid ${color}; margin-top: 15vh;`)


  div.

include /partials/scripts.pug
