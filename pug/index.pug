include /partials/variables.pug
doctype html
html(lang=`en`)
  head
    title Home | ikgron
    include /partials/head.pug
    include /partials/styles.pug
  
  style. 
    p {
      color: white;
    }
    .container {
      max-width: 50em;
      min-height: 11rem;
    }

body
  include /partials/navbar.pug
  div.container
    h1(style=`margin-left: 1rem`) Home

    h2(style=`margin-left: 1rem`) - A fine website by ikgron.

  hr

include /partials/scripts.pug
