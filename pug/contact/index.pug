include /partials/variables.pug
doctype html
html(lang=`en`)
  head
    title Contact | ikgron
    include /partials/head.pug
    include /partials/styles.pug

body
  include /partials/navbar.pug

  h1(style=`text-align: center;`) Contact Me!

  h2(style=`text-align: center;`) Email: 
    a.email(href=`mailto:contactme@ikgron.com`) contactme@ikgron.com

    br

    a(href=`https://codeberg.org/parser` target=`_blank` title=`Codeberg`)
      img.icon(
        src=`/images/codeberg.svg`
        alt=`Codeberg Icon`
      )

    a(href=`https://github.com/ikgron/` target=`_blank` title=`Github`)
      img.icon(
        src=`/images/github.png`
        alt=`Github Icon`
      )

  hr

include /partials/scripts.pug
