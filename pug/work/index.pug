include ../partials/variables.pug
doctype html
head
    title Work | ikgron
    include ../partials/head.pug
    include /partials/styles.pug

.bg
.content
  include ../partials/navbar.pug

  h1.text-light(style=`margin-top: 15vh; font-size: ${lfont}; margin-left: 2vh`) My Work

  h1.text-light(style=`margin-top: 1vh; font-size: ${sfont}; margin-left: 2vh`) - 
    a(href='https://codeberg.org/parser/', target='_blank' style='text-decoration: underline; color: ${color};') Codeberg

  h1.text-light(style=`margin-top: 1vh; font-size: ${sfont}; margin-left: 2vh`) - 
    a(href='https://github.com/ikgron', target='_blank' style='text-decoration: underline; color: ${color};') Github

  div(style=`border-bottom: 2px solid ${color}; margin-top: 15vh;`)


include ../partials/scripts.pug

