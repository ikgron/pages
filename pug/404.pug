include /partials/variables.pug
doctype html
html(lang=`en`)
  head
    title Page Not Found
    include /partials/head.pug
    include /partials/styles.pug

body
  include /partials/navbar.pug

  h1.text-light(style=`margin-left: 1rem`) 404

  h2.text-light(style=`margin-left: 1rem`) - Are you sure you`re in the right place? Would you like to 
    a(href=`/`, style=`text-decoration: underline; color: #{color};`) go Home
    | ?

  hr

include /partials/scripts.pug
